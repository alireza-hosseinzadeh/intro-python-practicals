# Data folder

Place any datasets used by the notebooks in this folder.

Use relative paths in notebooks, for example:

```python
import pandas as pd

df = pd.read_csv("../data/example_dataset.csv")
```
